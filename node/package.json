{
  "name": "@cabaret/node",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "napi build --platform --manifest-path ../lib/Cargo.toml --features napi --no-const-enum -o .",
    "build:release": "napi build --platform --manifest-path ../lib/Cargo.toml --features napi --no-const-enum -o . --release"
  },
  "devDependencies": {
    "@napi-rs/cli": "catalog:"
  },
  "napi": {
    "binaryName": "cabaret",
    "dtsHeader": "/* auto-generated by NAPI-RS */\n/* eslint-disable */\nexport type ChangeId = string\nexport type Identity = string\nexport type RepoPath = string\nexport type Revision = string\nexport type SessionId = string\nexport type WorkspaceId = string\n"
  }
}
