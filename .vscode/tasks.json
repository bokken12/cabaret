{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build extension",
      "type": "shell",
      "command": "pnpm --dir node build && pnpm --dir vscode compile",
      "problemMatcher": "$tsc",
      "group": "build"
    }
  ]
}
